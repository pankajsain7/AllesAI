{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --webpack -H 127.0.0.1",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "check:models": "node scripts/check-models.mjs",
    "check:ssrf": "node --import tsx scripts/check-ssrf.mjs",
    "check:persistence": "node --import tsx scripts/check-persistence.mjs",
    "check:hydration-race": "node --import tsx scripts/check-hydration-race.mjs",
    "check:bedrock-toggle": "node --import tsx scripts/check-bedrock-toggle.mjs",
    "check:bedrock-selection": "node --import tsx scripts/check-bedrock-selection.mjs",
    "check:key-forwarding": "node --import tsx scripts/check-client-key-forwarding.mjs",
    "check:consensus": "node --import tsx scripts/check-consensus.mjs",
    "check:consensus:live": "node --import tsx scripts/check-consensus.mjs --live",
    "check:chat": "node scripts/check-chat.mjs",
    "check:all": "npm run check:models && npm run check:ssrf && npm run check:persistence && npm run check:hydration-race && npm run check:bedrock-toggle && npm run check:bedrock-selection && npm run check:key-forwarding && npm run check:consensus"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "highlight.js": "^11.11.1",
    "lucide-react": "^1.14.0",
    "next": "16.2.6",
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "react-markdown": "^10.1.0",
    "rehype-highlight": "^7.0.2",
    "remark-gfm": "^4.0.1",
    "tailwind-merge": "^3.6.0",
    "zustand": "^5.0.13"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "@types/node": "^25.7.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9.39.4",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4.3.0",
    "tsx": "^4.23.13",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "keyv": "^5.6.0"
  }
}
